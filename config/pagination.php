<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Pagination Per Page Options
    |--------------------------------------------------------------------------
    |
    | This value defines the available options for items per page in
    | paginated results. Users can select from these values when viewing
    | paginated data.
    |
    */

    'per_page_options' => [5, 10, 15, 25, 50, 100, 200],

    /*
    |--------------------------------------------------------------------------
    | Default Per Page
    |--------------------------------------------------------------------------
    |
    | This value defines the default number of items to display per page
    | when no per_page parameter is provided in the request.
    |
    */

    'default' => 15,

];
